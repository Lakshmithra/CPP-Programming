#include<iostream>                                          // This is a header file that allows input and output (I/O) using streams (cin and cout)

int main() {                                             // Main function - the starting point of every C++ program
    std::cout << "Hello World !";                       // Output statement that prints "Hello World !" to the screen
    return 0;                                          // Ends the program and returns 0 to the operating system (means successful execution)
}
